<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springboard.Mapper.UserMapper">
    <insert id="insertUser" parameterType="com.example.springboard.DTO.User">
        INSERT INTO user (
        u_id,
        u_password,
        u_name,
        u_email,
        u_phone
        ) VALUES (
        #{u_id},
        #{u_password},
        #{u_name},
        #{u_email},
        #{u_phone}
        )
    </insert>

    <select id="findById" parameterType="com.example.springboard.DTO.User">
        select * from user where u_id = #{u_id} and u_password = #{u_password}
    </select>
</mapper>